Resolve `<WORKSPACE_ROOT>` to the root of the current checkout. Use `$ai-drama-script-audit` by reading `<WORKSPACE_ROOT>/.agents/skills/ai-drama-script-audit/SKILL.md`.

用户任务：请对 `<WORKSPACE_ROOT>/tests/forward-fixtures/audit-input.md` 中的 v001 做证据化符合性审计和开放诊断。不要改写剧本。把报告写入 `<WORKSPACE_ROOT>/tests/.forward-runs/audit/audit-report-v001.md`，默认简体中文，并在最终消息中只报告路径与门禁结论。
